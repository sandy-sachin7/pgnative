# Benchmarks — per AGENTS §4.3

Record hw, OS, PG version, data shape, query, network, build mode, caches warm/cold alongside numbers.

Fixtures: 10k/100k/500k/1m rows, wide, large-text, JSONB, mixed types. See §36.4.

To run: no Criterion benches exist yet. When added, run `cargo bench -p pgnative-results`.
